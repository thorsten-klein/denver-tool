# This file contains specifies dependencies that are conflicting, for example if a package uses pinned versions.
# We relax those dependency versions on our own risk.

# Get dependencies via:
#     pip show codechecker
# or
#     pipdeptree -p codechecker -d 1
#
# Get available versions via
#     pip index versions <package>

# --------------------------------------
# relax pinned versions from codechecker
# --------------------------------------
alembic==1.17.2
Authlib==1.6.6
gitpython==3.1.45
lxml==5.4.0 # must be <6
multiprocess==0.70.18
portalocker==3.2.0
psutil==7.1.3
PyYAML==6.0.3
requests==2.32.5
sarif-tools==3.0.5
setuptools==80.9.0 # must be <81
sqlalchemy==1.4.54 # must be <2
thrift==0.22.0
types-PyYAML==6.0.12.20250915
grpcio-tools==1.76.0
