# Shared by build_picotool.sh ('build-picotool's cmd:) and
# activate_picotool.sh (its source:), so the build dir and the install
# prefix can't drift apart between them.
PICOTOOL_BUILD_DIR="${DENVER_ENV_WORKDIR}/picotool-build"
PICOTOOL_INSTALL_DIR="${DENVER_ENV_WORKDIR}/picotool-install"
